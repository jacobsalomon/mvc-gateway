import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Rewrites are in vercel.json for Vercel edge-level routing
  // to the AeroVision demo and investor pitch sub-apps
};

export default nextConfig;
